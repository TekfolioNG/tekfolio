// app.config.ts
export default defineAppConfig({
  nuxtIcon: {
    class: "",
  },

  ui: {
      primary: '#D2410C',        // Uses the primary palette directly
      secondary: 'secondary',    // Uses the secondary palette directly
      success: 'green',
      info: 'blue',
      warning: 'amber',
      error: 'red',
      neutral: 'slate'
  }
});