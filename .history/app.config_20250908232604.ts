// app.config.ts
export default defineAppConfig({
  nuxtIcon: {
    class: "",
  },

  ui: {
    colors: {
      brandprimary: 'orange',        // Uses the primary palette directly
      secondary: 'secondary',    // Uses the secondary palette directly
      success: 'green',
      info: 'blue',
      warning: 'amber',
      error: 'red',
      neutral: 'slate'
    }
  }
});